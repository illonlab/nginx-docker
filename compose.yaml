services:
  nginx:
    image: nginx:latest
    restart: unless-stopped
    network_mode: host
    env_file: .env
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./fastcgi_params:/etc/nginx/fastcgi_params:ro
      - ./scgi_params:/etc/nginx/scgi_params:ro
      - ./uwsgi_params:/etc/nginx/uwsgi_params:ro
      - ./mime.types:/etc/nginx/mime.types:ro
      - ./dhparam.pem:/etc/nginx/dhparam.pem:ro
      - ./templates:/etc/nginx/templates:ro
      - ./conf.d:/etc/nginx/conf.d
      - ./stream-conf.d:/etc/nginx/stream-conf.d
      - ./locations:/etc/nginx/locations
      - ./ssl:/etc/letsencrypt
      - ./99-autoreload.sh:/docker-entrypoint.d/99-autoreload.sh:ro
      - ./20-envsubst-on-templates.sh:/docker-entrypoint.d/20-envsubst-on-templates.sh:ro
      - ./www/html:/var/www/html
      - ./www/certbot:/var/www/certbot

  certbot:
    image: certbot/certbot
    volumes:
      - ./ssl:/etc/letsencrypt
      - ./www/certbot:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
networks: {}
